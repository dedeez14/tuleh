<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- WAJIB agar build RELEASE punya akses jaringan. Flutter hanya menaruh
         izin ini di manifest debug/profile secara default → tanpa baris ini,
         APK rilis TAK bisa memanggil API (login & semua request gagal senyap). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Auto-Update: memasang APK pembaruan dari dalam app (sumber tak dikenal). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <!-- Cetak struk ke printer thermal Bluetooth. Target perangkat: Android 10+
         (minSdk 29), jadi DUA generasi izin harus ada berdampingan:

         • Android 10 & 11 (API 29-30) memakai BLUETOOTH + BLUETOOTH_ADMIN.
           Keduanya izin "normal" — diberikan saat pemasangan, tanpa dialog.
         • Android 12+ (API 31) memakai BLUETOOTH_CONNECT (runtime).

         Aplikasi hanya membaca perangkat yang SUDAH dipasangkan, tidak
         memindai. Karena itu BLUETOOTH_SCAN tidak diminta di sini (plugin
         mendeklarasikannya sendiri) dan izin LOKASI tidak diperlukan — di
         Android 10/11 justru pemindaianlah yang menuntut lokasi. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- BLUETOOTH_SCAN dideklarasikan plugin printer & diminta saat izin
         printer; posisi TIDAK pernah diturunkan dari hasil pindai, jadi
         dinyatakan neverForLocation (Android 12+ tak lagi menganggapnya izin
         lokasi). -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-feature android:name="android.hardware.bluetooth" android:required="false"/>

    <!-- Pemantau pesanan meja (layanan latar depan, flutter_foreground_task):
         memeriksa /bills & /orders berkala walau aplikasi di latar belakang,
         lalu memberi notifikasi bersuara saat ada pesanan/permintaan bayar
         baru. Android 10 (minSdk 29) cukup FOREGROUND_SERVICE; Android 14+
         menuntut tipe dataSync; Android 13+ menuntut POST_NOTIFICATIONS. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Cadangan Android DIMATIKAN: token sesi (Keystore), antrean penjualan
         offline (SQLite), salinan data, dan laporan diagnostik tidak boleh
         ikut ke cadangan cloud / pindah perangkat. Kunci Keystore tidak ikut
         dipulihkan, sehingga data terenkripsi hasil pulihan justru merusak
         aplikasi; antrean yang terpulihkan di perangkat lain bisa mengirim
         penjualan ganda. allowBackup=false menutup cadangan cloud & adb;
         dataExtractionRules (Android 12+) juga menutup transfer antarperangkat. -->
    <application
        android:label="Tuléh"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="s">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Pemantau pesanan meja. Nama service TIDAK boleh diubah (milik plugin).
             stopWithTask=false: tetap hidup saat aplikasi digeser dari recent. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false"
            android:exported="false" />
        <!-- Penerima boot/pembaruan paket milik flutter_foreground_task diekspor
             plugin (exported=true) sehingga aplikasi lain bisa mengirim siaran
             palsu untuk menyalakan layanan latar. Siaran BOOT_COMPLETED /
             MY_PACKAGE_REPLACED dikirim SISTEM, dan penerima non-ekspor tetap
             menerima siaran sistem — jadi cukup ditutup dari aplikasi lain. -->
        <receiver
            android:name="com.pravera.flutter_foreground_task.service.RebootReceiver"
            android:exported="false"
            tools:replace="android:exported" />
        <!-- Auto-Update: berbagi berkas APK yang diunduh ke pemasang sistem. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: membuka tautan perpanjang langganan & WhatsApp CS
             (alamat dari server) di aplikasi luar. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
